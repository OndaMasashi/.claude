# 並列化・自動化・スケジュール 選択ガイド

> CLAUDE.md から外部化したオンデマンド参照。**実装計画・並列化を検討する時**および**将来 TODO のスケジュール化を検討する時**に Read する。
> Plan Mode 突入時は `plan-brainstorm.sh` フックが本書の読込を促す。
> CLAUDE.md 側には「判断の核」だけ残してある。詳細な比較・適用条件は本書を正とする。
>
> **バージョン注記の見方**: `✅導入済み` / `⏳未到達` は **2026-08-29 時点・CLI 2.1.220** での判定。
> `autoUpdatesChannel: latest` で自動更新されるため、更新後は印を見直すこと。
> **印のない機能は確認していない**（動く保証も動かない証拠もない）。

## 手段の選択（並列化・自動化）

プランモード等でタスクの実装を計画する際、並列化・自動化手段を以下から選択すること。

| 手段 | 適用条件 | コスト | 注意 |
| :--- | :--- | :--- | :--- |
| サブエージェント (`.claude/agents/`) | 独立した並列タスク、結果だけ親に返す | 低 | 双方向通信は不可。**指示文に書いたことしか伝わらない**（文脈の切り出しが必要）。セッションあたり200体の spawn 上限は v2.1.224 で撤廃（同時実行数と入れ子深さの制限は残る）⏳未到達。対話セッションでの非チームメイト spawn は v2.1.232 以降デフォルト BG 実行 ⏳未到達 |
| **fork サブエージェント** (`subagent_type: "fork"`) | 親の会話文脈をそのまま持たせたい。前提が長く指示文に書き起こすのが非現実的／**同じ文脈から別解を試す**（比較・やり直し） | 低（プロンプトキャッシュを継承するため再送分が安い） | **⏳未到達（2026-08-29 実測）**: 2.1.220 で `subagent_type: "fork"` を起動すると `Agent type 'fork' not found` になる。Agent ツールの説明文には fork の記載があるが**エージェント種別としては未登録**なので、説明文を根拠に使えると判断しないこと。以下は到達後の仕様: v2.1.232 でデフォルト on。**親モデルを必ず継承**（`model` 指定は無視される）。無関係なタスクに使うと不要な文脈を丸ごと運ぶので割高。通常のサブエージェントとの違いは「文脈を切り出すか、丸ごと渡すか」 |
| バックグラウンドエージェント (`claude --bg` / `claude agents`) | タスクを引き継いで後で結果を確認、複数並行 | 低 | `claude agents` で一覧管理（`/agents` とは別物。後者は現セッション内のサブエージェント管理）。v2.1.221 以降、BG セッションは成果保全のため commit/push し、必要な時だけ draft PR を開き、**最後に成果物の置き場所を必ず報告**する ⏳未到達 |
| セッション間メッセージング (`SendMessage` / `ListAgents` / プロンプトの `@`) | **既に走っている別セッション**（同一マシン・他マシン・クラウド）に依頼や質問を渡す。新規に並列化するのではなく既存セッションの再利用 | 低 | **✅一覧取得は Windows・2.1.220 で動作**（2026-08-29 実測。`ListAgents` がローカル7・Remote Control 7 の計14セッションを列挙）。**送信（`SendMessage`）の到達は未検証**。以前あった「Windows は v2.1.239 で対応」という記述は観測と合わないため撤回した。受信側は `crossSessionInbound`（accept/hold/refuse）で制御。`notify_when_idle` で「手が空いたら一度だけ通知」（ポーリング不要）。送信失敗・受信拒否は v2.1.238 以降きちんと送信側に返る（それ以前は黙って消えていた）⏳未到達＝**現状は送信が黙って消える可能性がある**。クラウドセッションは受け取れるが**返信はできない** |
| Agent Teams | 協調・議論が必要、クロスレイヤー調整、仮説の反論 | 高（人数分） | ペルソナを明示（財務/営業/IT運用/データサイエンス/製造/総務/法務/R&D 等）。指揮者は戦略・統括として振舞う。**チーム内では worktree 分離されない**ためファイルを別々に割り当てる |
| 動的ワークフロー (`ultracode` キーワード〔旧 `workflow`、v2.1.160 でリネーム✅導入済み・入力欄で violet ハイライト〕 / `/effort ultracode` / `/workflows`) | **大量（数十〜数百）×段取り確定×再現したい**。コードベース監査・大規模マイグレーション・相互検証研究。サブエージェントを JS スクリプトで決定論的にオーケストレーション（指揮者がコード）。中間結果はスクリプト変数に隔離されメイン文脈を汚さない／BG実行で応答性維持／一時停止・再開可／保存して `/<name>` 再実行 | 高（多エージェント、プラン使用量・レート制限にカウント） | サブエージェントの兄弟（横通信なし・一方向制御）。Agent Teams と違い**議論・対話・実行中介入は不可**（権限プロンプトのみ停止可）。リサーチプレビュー（v2.1.154+、全有料プラン、Pro は `/config` でオン）✅導入済み。同時16/累計1000上限。`isolation: 'worktree'` で並列編集の競合を内部回避可。各エージェントは段取りに従うが**作業の中身は LLM の自由（schema/プロンプト/モデル振分で締付可）** |
| ワークツリー (`--worktree` / `EnterWorktree`) | サブエージェント・並列セッションが同じファイルを編集する可能性 | 低 | 他の手段と組み合わせて使う分離装置。`worktree.baseRef`（`fresh`/`head`）で派生元制御。`EnterWorktree` はセッション途中で Claude 管理 worktree を切替可（v2.1.157+）✅導入済み |
| `/batch` コマンド | リポジトリ全体マイグレーション、機械的リファクタリング（5〜30 個のワークツリー分離サブエージェントを自動オーケストレーション、各 PR を開く） | 中（並列分の API） | 1 つの指示で説明できる範囲に限る |
| Agent SDK (Python/TS) | 人間不在の定期実行、JSON 出力を後段に渡す、API 化、CLI 以外のトリガー（Webhook/cron/CI）、複数スキルを組み合わせたパイプライン | API 従量課金 | claude.ai (Max/Pro) 認証は使用不可。詳細: `c:/work/CLAUDE_docs/note/Agent-SDK概要.md` |

**判断軸**: ① 誰が調整するか（自分=Agent Teams / Claude がターンごと=サブエージェント / スクリプト=動的ワークフロー）② ワーカー間通信の要否（要=Agent Teams、不要=サブエージェント・ワークフロー）③ 同じファイルを編集するか（→ ワークツリーで分離）④ **今の文脈をどれだけ渡すか**（切り出して渡す=通常のサブエージェント / 丸ごと渡す=fork / そもそも別セッションに投げる=セッション間メッセージング）。採用/不採用それぞれの理由を明示すること。

**サブエージェント vs 動的ワークフローの閾値**: デフォルトはサブエージェント。**「10体以上 ＆ 段取りが確定 ＆ また使う（再現性）」が揃ったら動的ワークフロー**。1つでも欠ければサブエージェント＋手動オーケストレーションで足りる（プレビュー機能のコスト・リスクを避ける）。決め手は「**この段取り、また回すか**」。さらに「相互検証・多角ドラフト等の品質パターンを強制したい」「中間結果が大量でメイン文脈を汚したくない」「長時間BGで回したい」のいずれかもワークフロー昇格トリガー。

新規開発・機能追加・リファクタリング・ロードマップ等の **開発計画** を立てる際は `test-strategy` スキルを参照し、テスト戦略（ピラミッドのバランス・静的解析・CI・補強領域の採否）を計画段階から織り込むこと。

## 自動化・スケジュール化の選択肢

タスク完了時に「N 日後 / 定期的に X を確認」のような将来 TODO が残ったら、以下を必ず候補として提示すること:

- **CCR (`/schedule` claude.ai routine)**: PC OFF でも Anthropic クラウドで動く / GitHub 連携で repo・Actions・PR を直接操作 / cron は 1 時間以上の間隔 / claude.ai (Max/Pro) 認証で API 課金なし。**典型ユース**: 「2 週間後に運用状況をレビューして follow-up を ROADMAP に追加」「毎週月曜に PR を triage」「リリース後の metrics を 1 週間後に確認」など、ローカル環境を要しない future-me タスク
- ローカル PC や local file / 未連携 MCP に依存するタスクは Agent SDK / cron を選ぶ（CCR では実行不可）

**継続実行系の使い分け**: `/loop`（時間間隔ベース）/ `/goal`（**完了条件達成まで自律継続**、各ターン後に小型モデルが判定）/ バックグラウンドエージェント（時間軸の分離・引き継ぎ型）/ CCR routine（クラウド・PC OFF 対応）。ローカル依存なら前3つ、依存なしなら CCR を優先。`/goal` は完了基準が曖昧だと暴走するため明確化必須。v2.1.234〜239 で挙動が改善 ⏳未到達（以下は到達後の話。2.1.220 では効かない）: 長時間の BG 作業待ちは **30分 → 1時間 → 2時間** とバックオフしながら自動チェックイン（`CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0` で無効化）、`claude --resume` で goal が復元、認証失効・クレジット切れ・コンテキスト超過など回復不能なエラーでは自動解除される。
