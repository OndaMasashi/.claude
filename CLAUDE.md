# Global Claude Code Settings

## 環境・参照先

### Accounts

- GitHub: [OndaMasashi](https://github.com/OndaMasashi)

### Antigravity 連携

- グローバル設定: `C:\Users\Masashi.Onda\.agent\global.md`
- このファイル（CLAUDE.md）の設定を変更したら、Antigravity 側の global.md にも同等の変更を反映すること

### スキル

スキルは `~/.claude/skills/` 配下から Claude Code が自動発見し、Skill ツール経由で発火する（ネイティブ経路）。実体は `C:\work\utility\skills-main\skills-main\skills\` で、各スキル毎に junction で参照されている。

- 新規追加・リネーム・削除を行ったら必ず `pwsh C:\work\utility\skills-main\skills-main\skills\sync_skill_junctions.ps1` で junction を同期する
- 人間用閲覧資料 `~/.claude/skills-catalog.md` は保守しているが、システムプロンプトへの auto-include はしない（L1 コンテキスト削減）。必要時のみ Read で参照
- 詳細な運用ルールはリポジトリの `c:\work\utility\skills-main\CLAUDE.md` を参照

### ナレッジ参照先

- Claude活用事例: `c:\work\CLAUDE_docs\claude_resources\use-cases\` (85件) — 類似ユースケースのプロンプト例・機能・ワークフローの参考に

### 技術バージョン情報

FW・ライブラリ・ランタイム（Next.js, React, Vue, Node.js, Python, TypeScript 等）のバージョンに言及する／コードを生成する際は、必ず @~/.claude/tech-versions.md を参照すること。

- **モデル訓練時の知識で「最新版」を判断しない**。Next.js 15 と 16、React 18 と 19 のような取り違いは典型的な事故
- リストにない技術は `npm view <pkg> version` / `pip index versions <pkg>` で都度確認
- **検出した未追跡の技術は `~/.claude/tech-versions-sources.tsv` に1行追加**（`category<TAB>name<TAB>command` 形式、command は stdout に値1行だけ吐くシェルパイプ）してから `bash ~/.claude/scripts/update-tech-versions.sh` を実行。tech-versions.md は自動再生成される
- リストの `last_updated` が **14日以上前** ならユーザーに警告し、手動更新を促す
- バージョン依存の API（App Router, RSC, Suspense 等）を使う前に必ずリスト確認
- リストは毎週日曜 00:00 に Windows タスクスケジューラで自動更新される

### RAG Knowledge Base

中小企業診断士・経営支援・農業支援・AI・技術ナレッジ に関する質問は、まず reverve-rag の `semantic_search` で検索すること。以下は `semantic_search` 呼び出しのルール（コード探索 Grep/Glob 等には適用しない）:

- 最初の検索結果が不十分な場合、クエリを言い換えて再検索する
- 複雑な質問は複数の観点に分解し、それぞれで検索する
- `domain` フィルタを活用し、関連ドメインも横断的に確認する
- 検索結果の情報量が質問に対して十分か自己評価し、不足なら追加検索する
- 呼び出しは **1つの質問につき最大3回まで** とし、それでも不足する場合はその旨をユーザーに伝える

## タスク運用

### 説明・報告の方針

Opus 4.8 は深い専門用語を多用する傾向がある。読み手の負担を下げるため、全ての説明・報告で:

- **領域外の専門用語にはカッコで1行の言い換えを添える**（例: 「N+1 問題（件数に比例して DB アクセスが増え遅くなる現象）」）
- **技術的な事象は業務・運用への影響を一文添える**（例: 「null 安全違反 → 特定条件でアプリが落ちる」）
- **略語は初出時に展開**（LSP / MCP / RSC / SSRF 等）

例外: ユーザーが「専門用語のままで」と明示した場合はそのまま使ってよい。

### タスク完了時の振り返り

**コード変更があった場合**: 完了報告の前に `/refine` を実行（対象: 今回変更・新規作成したファイル。調査のみ・計画のみの場合はスキップ）。`/refine` は内部で「`code-simplifier` agent による品質改善（修正適用）→ `/code-review` による正確性バグ検出（報告のみ）」を順次実行する。

**得た知見の置き場所**（外部化の理由: 探索・試行錯誤の過程はコンテキストを汚染するため、結論だけ分離した方が性能が高い）:

| 得たもの | 置き場所 | 動作 |
| :--- | :--- | :--- |
| 試行錯誤の結果・進捗 | `memory/` | 自動記録（提案不要） |
| 繰り返し使えるルール・制約 | `CLAUDE.md` または `.claude/rules/` | 追記を提案 |
| プロジェクト固有だが汎用化できる設定 | `~/.claude/`（グローバル昇格） | 提案 |
| 計画の結論・仕様 | `SPEC.md` / `plan.md` 等 | ファイルに書き出し、新セッションで `@ファイル名` 参照 |

memory/ への記録は既存と重複しないか確認し、簡潔に。軽微なタスクでは不要。提案も簡潔に。

### 改修履歴の管理

プログラムの改修を行った際、プロジェクトフォルダ配下の `improvement_list/` に履歴を記録すること。

- ファイル名: `YYYY-MM-DD_{短い説明}.md`（例: `2026-03-01_fix_login_validation.md`）
- 1プラン（計画単位）＝1ファイル。プラン内の複数ファイル変更はまとめて1つの履歴に記録する
- 各ファイルに記載: **対象**（変更したファイルパス、複数可）／ **変更内容**（何をどう変えたか、簡潔に）／ **理由**（なぜ必要だったか）
- 軽微な修正（誤字・フォーマット等）は記録不要
- 未完了のタスクがあれば「残作業」セクションを追記する

### モデル開発リファレンス

**新規 ML/DL モデル開発（Kaggle コンペ / 業務 PoC / 研究プロジェクト等）のキックオフ時にのみ**、`~/.claude/model-dev-playbook.md` を Read ツールで読み込んで参照すること。**`@` プレフィックスでの auto-include は使わない**（毎セッションでコンテキストを汚染するため）。

- **読み込みトリガー**: 「新規 ML プロジェクト」「新コンペ着手」「新規モデル開発」「Phase 0 から立ち上げ」等の指示を受けた時点で能動的に Read
- **適用条件**: 新規モデル開発の Phase 0 立ち上げ前（問題定義 → pipeline 設計の段階）
- **不要なケース**: 既存 codebase の改修 / バグ修正 / リファクタリング / 運用作業 → Read 不要
- **内容概要**: 問題タイプ診断 (A-E) / 5 レイヤー構造 (Infra/Signal/Uncertainty/Stack/Post-proc) / タイプ別 pipeline template / アンチパターン / プロセスメタ (速度チェック・動向スイープ・抽象化階層見直し)
- **適用判断**: 既存 codebase 改修中に「その前段の pipeline 設計が未整備」と判明したら、ユーザーに本リファレンス参照を提案

## 計画・設計

### 手段の選択（並列化・自動化）

プランモード等でタスクの実装を計画する際、並列化・自動化手段を以下から選択すること。

| 手段 | 適用条件 | コスト | 注意 |
| :--- | :--- | :--- | :--- |
| サブエージェント (`.claude/agents/`) | 独立した並列タスク、結果だけ親に返す | 低 | 双方向通信は不可 |
| バックグラウンドエージェント (`claude --bg` / `claude agents`) | タスクを引き継いで後で結果を確認、複数並行 | 低 | `claude agents` で一覧管理（`/agents` とは別物。後者は現セッション内のサブエージェント管理） |
| Agent Teams | 協調・議論が必要、クロスレイヤー調整、仮説の反論 | 高（人数分） | ペルソナを明示（財務/営業/IT運用/データサイエンス/製造/総務/法務/R&D 等）。指揮者は戦略・統括として振舞う。**チーム内では worktree 分離されない**ためファイルを別々に割り当てる |
| 動的ワークフロー (`workflow` キーワード / `/effort ultracode` / `/workflows`) | **大量（数十〜数百）×段取り確定×再現したい**。コードベース監査・大規模マイグレーション・相互検証研究。サブエージェントを JS スクリプトで決定論的にオーケストレーション（指揮者がコード）。中間結果はスクリプト変数に隔離されメイン文脈を汚さない／BG実行で応答性維持／一時停止・再開可／保存して `/<name>` 再実行 | 高（多エージェント、プラン使用量・レート制限にカウント） | サブエージェントの兄弟（横通信なし・一方向制御）。Agent Teams と違い**議論・対話・実行中介入は不可**（権限プロンプトのみ停止可）。リサーチプレビュー（v2.1.154+、全有料プラン、Pro は `/config` でオン）。同時16/累計1000上限。`isolation: 'worktree'` で並列編集の競合を内部回避可。各エージェントは段取りに従うが**作業の中身は LLM の自由（schema/プロンプト/モデル振分で締付可）** |
| ワークツリー (`--worktree` / `EnterWorktree`) | サブエージェント・並列セッションが同じファイルを編集する可能性 | 低 | 他の手段と組み合わせて使う分離装置。`worktree.baseRef`（`fresh`/`head`）で派生元制御。`EnterWorktree` はセッション途中で Claude 管理 worktree を切替可（v2.1.157+） |
| `/batch` コマンド | リポジトリ全体マイグレーション、機械的リファクタリング（5〜30 個のワークツリー分離サブエージェントを自動オーケストレーション、各 PR を開く） | 中（並列分の API） | 1 つの指示で説明できる範囲に限る |
| Agent SDK (Python/TS) | 人間不在の定期実行、JSON 出力を後段に渡す、API 化、CLI 以外のトリガー（Webhook/cron/CI）、複数スキルを組み合わせたパイプライン | API 従量課金 | claude.ai (Max/Pro) 認証は使用不可。詳細: `c:/work/CLAUDE_docs/note/Agent-SDK概要.md` |

**判断軸**: ① 誰が調整するか（自分=Agent Teams / Claude がターンごと=サブエージェント / スクリプト=動的ワークフロー）② ワーカー間通信の要否（要=Agent Teams、不要=サブエージェント・ワークフロー）③ 同じファイルを編集するか（→ ワークツリーで分離）。採用/不採用それぞれの理由を明示すること。

**サブエージェント vs 動的ワークフローの閾値**: デフォルトはサブエージェント。**「10体以上 ＆ 段取りが確定 ＆ また使う（再現性）」が揃ったら動的ワークフロー**。1つでも欠ければサブエージェント＋手動オーケストレーションで足りる（プレビュー機能のコスト・リスクを避ける）。決め手は「**この段取り、また回すか**」。さらに「相互検証・多角ドラフト等の品質パターンを強制したい」「中間結果が大量でメイン文脈を汚したくない」「長時間BGで回したい」のいずれかもワークフロー昇格トリガー。

新規開発・機能追加・リファクタリング・ロードマップ等の **開発計画** を立てる際は `test-strategy` スキルを参照し、テスト戦略（ピラミッドのバランス・静的解析・CI・補強領域の採否）を計画段階から織り込むこと。

### 自動化・スケジュール化の選択肢

タスク完了時に「N 日後 / 定期的に X を確認」のような将来 TODO が残ったら、以下を必ず候補として提示すること:

- **CCR (`/schedule` claude.ai routine)**: PC OFF でも Anthropic クラウドで動く / GitHub 連携で repo・Actions・PR を直接操作 / cron は 1 時間以上の間隔 / claude.ai (Max/Pro) 認証で API 課金なし。**典型ユース**: 「2 週間後に運用状況をレビューして follow-up を ROADMAP に追加」「毎週月曜に PR を triage」「リリース後の metrics を 1 週間後に確認」など、ローカル環境を要しない future-me タスク
- ローカル PC や local file / 未連携 MCP に依存するタスクは Agent SDK / cron を選ぶ（CCR では実行不可）

**継続実行系の使い分け**: `/loop`（時間間隔ベース）/ `/goal`（**完了条件達成まで自律継続**、各ターン後に小型モデルが判定）/ バックグラウンドエージェント（時間軸の分離・引き継ぎ型）/ CCR routine（クラウド・PC OFF 対応）。ローカル依存なら前3つ、依存なしなら CCR を優先。`/goal` は完了基準が曖昧だと暴走するため明確化必須。

### Plan Mode

Plan Mode で計画を作成する際、不明点や曖昧な要件があれば AskUserQuestion ツールを積極的に使用してユーザーに確認すること。推測で進めず、要件・制約・優先度を明確にしてから計画を立てること。

#### ブレスト・フェーズ（Plan Mode 自動適用）

実装計画に入る前に以下のブレスト・フェーズを踏むこと:

1. **目的・制約の確認**: いきなり「どう作るか」に入らず、まず「なぜ作るか」「成功基準は何か」を確認する
2. **アプローチ比較**: 2-3の実現方法を洗い出し、トレードオフを比較してから1つに絞る
3. **段階的な質問**: 質問は1問ずつ行い、一度に複数の質問を投げない。選択肢を提示できる場合は選択式にする
4. **YAGNI の徹底**: 設計段階で不要な機能・拡張ポイントを削ぎ落とす

スキップ条件: バグ修正など設計余地がない単純作業 ／ ユーザーが「ブレスト不要」「計画だけ」と明示した場合

#### 新規プロダクト/アプリ設計の最上流（開発着手前の3点）

実装コストが下がった現在、差を生むのは「何をどんな思想で作るか」という戦略。**新規プロダクト・新機能・新サービスの起案時、および開発中に方向性へ迷った時に**以下3点を決めるようユーザーに促すこと。その結果をConceptとしてアウトプット(md等)に残すこと。

1. **誰のために作るか（ターゲットと本質的欲望）**: 「みんな向け」は誰にも刺さらない凡庸（mediocrity）になる。ターゲットを尖らせ一部に熱狂される設計にする／「どんな人が結果どう反応・行動するか」をゴールとして1文で言語化（解像度を上げる）／表面課題（例: 効率化したい）で止めず「その先に何を得て・どう幸福になりたいか」まで深掘り。
2. **どこまで作れば価値が出るか（ゴールラインと PRFAQ）**: 範囲の無限膨張を防ぐ境界線を先に引く。未完成製品の発表文（プレスリリース）＋想定 Q&A（FAQ）を着手前に書き、技術・予算制約を離れ「顧客価値」だけに集中する／その後で競合を分析し、選ばれるのに必要な品質水準（or 唯一性だけで成立する領域か）を相対判断。
3. **何を優先するか（デザインプリンシプル）**: 開発中に各所から入る要望を全部聞いて中途半端化するのを防ぐ判断基準。最重要軸（速さ/シンプルさ/機能数/プライバシー等）を事前に決める／「ユーザー第一」式のスローガンでなく「速さと機能が対立したら速さを採る」のように迷った時そのまま使える形にする。

#### 未経験領域では設計前に「定石調査」

新 FW・新ライブラリ・新業界等の未経験領域は、自分のドメイン論理から設計する前に、標準パターン / 実プロダクト数件 / 公式推奨を調査して**選択肢リストを広げる**。理由: **「分からない問題」（試行錯誤で解ける）と「知らない手法」（選択肢に無いと永遠に出ない）は別物**。

- Agent Team: ドメイン専門家に加え**「メタ専門家」ロール**（領域定石を列挙する人格）を 1 名常設
- 失敗実験は close 前に**同じ素材の別の使い方を 3 種**列挙してから判定

## 実行時の基本ルール

- **成功条件を先に定義する**: 着手前に「何が揃えば完了か」を1〜3行で明示する。2回試して満たせなければ状況報告して判断を仰ぐ
- **コンテキスト圧迫を予告する**: 長大な作業に入る前に「これは長い」と明示し、分割実行を提案する。残量が苦しくなったら続行せず要約してセッション分割を提案する
- **矛盾は表面化する**: 2つのパターンが衝突した場合、平均せず一方を選んで理由を説明する。もう一方は整理対象として明示する
- **中間チェックポイント**: 長いワークフローでは、重要なステップ完了ごとに「完了済み・残り」を要約する
- **慣習に従う**: ドキュメントのフォーマット規則は好みより準拠を優先する。異議がある場合は明示する
- **失敗は大きく報告する**: 省略・スキップ・不確実性があれば「完了」と言わない。エラーは隠さない
