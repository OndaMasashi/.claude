# Global Claude Code Settings

## Accounts

- GitHub: [OndaMasashi](https://github.com/OndaMasashi)

## Antigravity

- グローバル設定: `C:\Users\Masashi.Onda\.agent\global.md`
- このファイル（CLAUDE.md）の設定を変更した場合、Antigravity 側の global.md にも同等の変更を反映すること

## スキル

スキル一覧は @~/.claude/skills-catalog.md を参照。
ユーザーの指示がスキルに該当する場合、該当 SKILL.md を読み込んで従うこと。

## ナレッジ参照先

- Claude活用事例: `c:\work\CLAUDE_docs\claude_resources\use-cases\` (85件) — 類似ユースケースのプロンプト例・機能・ワークフローの参考に

## 技術バージョン情報

FW・ライブラリ・ランタイム（Next.js, React, Vue, Node.js, Python, TypeScript 等）のバージョンに言及する／コードを生成する際は、必ず @~/.claude/tech-versions.md を参照すること。

- **モデル訓練時の知識で「最新版」を判断しない**。Next.js 15 と 16、React 18 と 19 のような取り違いは典型的な事故
- リストにない技術は `npm view <pkg> version` / `pip index versions <pkg>` で都度確認
- **検出した未追跡の技術は `~/.claude/tech-versions-sources.tsv` に1行追加**（`category<TAB>name<TAB>command` 形式、command は stdout に値1行だけ吐くシェルパイプ）してから `bash ~/.claude/scripts/update-tech-versions.sh` を実行。tech-versions.md は自動再生成される
- リストの `last_updated` が **14日以上前** ならユーザーに警告し、手動更新を促す
- バージョン依存の API（App Router, RSC, Suspense 等）を使う前に必ずリスト確認
- リストは毎週日曜 00:00 に Windows タスクスケジューラで自動更新される

## RAG Knowledge Base

中小企業診断士・経営支援・農業支援・AI・技術ナレッジ に関する質問は、
まず reverve-rag の `semantic_search` で検索すること。

### 探索的RAG検索（semantic_search の使い方）

以下は `semantic_search` ツールの呼び出しに関するルール。
コード探索（Grep/Glob等）には適用しない。

- 最初の検索結果が不十分な場合、クエリを言い換えて再検索すること
- 複雑な質問は複数の観点に分解し、それぞれで検索すること
- `domain` フィルタを活用し、関連ドメインも横断的に確認すること
- 検索結果の情報量が質問に対して十分か自己評価し、不足なら追加検索すること
- `semantic_search` の呼び出しは **1つの質問につき最大3回まで** とし、それでも不足する場合はその旨をユーザーに伝えること

## タスク完了時の振り返り

タスク完了時、以下の観点で改善を行うこと：

- コード変更を伴う実装を行った場合、完了報告の前に `/simplify` を実行すること
  - 対象: 今回変更・新規作成したファイル
  - 調査のみ・計画のみの場合はスキップ
- 試行錯誤の結果や進捗 → memory/ に自動記録する（提案不要）
- 繰り返し使えるルールや制約 → CLAUDE.md or .claude/rules/ への追記を提案
- プロジェクト固有の設定が汎用的と判断できる場合 → グローバル（~/.claude/）への昇格を提案
- 計画の結論・仕様 → ファイル（例: SPEC.md, plan.md 等）に書き出す。
  探索・試行錯誤の過程はコンテキストを汚染するため、
  結論だけを外部化し、新セッションで `@ファイル名` 参照して実装に入る方が性能が高い

memory/ への記録は既存の記述と重複しないか確認し、簡潔に行うこと。
ただし軽微なタスクでは不要。提案も簡潔に。

## 改修履歴の管理

プログラムの改修を行った際、プロジェクトフォルダ配下の `improvement_list/` に履歴を記録すること。

- ファイル名: `YYYY-MM-DD_{短い説明}.md`（例: `2026-03-01_fix_login_validation.md`）
- 1プラン（計画単位）＝1ファイル。プラン内の複数ファイル変更はまとめて1つの履歴に記録する
- 各ファイルには以下を記載：
  - **対象**: 変更したファイルパス（複数可）
  - **変更内容**: 何をどう変えたか（簡潔に）
  - **理由**: なぜその変更が必要だったか
- 軽微な修正（誤字・フォーマット等）は記録不要
- 未完了のタスクがあれば「残作業」セクションを追記する

## 計画・設計時の視点

プランモード等でタスクの実装を計画する際、以下を検討すること：

- 並列化できるワークフロー（それぞれが独立で実施できる作業） → .claude/agents/ でのサブエージェント化
- 並列化できるワークフロー（協調して精度を上げるべき作業、または依存関係があり中間成果物の受け渡しが必要な作業）→ Agent Teams 化
- サブエージェント・Agent Teams の使用判断時は、採用/不採用それぞれの理由を明示すること（Agent Teamsの方がリソース消費が大）
- Agent Teamsを使用する場合、ペルソナとして、例えば、財務担当、営業・マーケティング担当、IT運用担当、AI・データサイエンティスト、製造・生産担当、総務担当、法務担当、R&D担当、等から適切な役割を選ぶ。Agentの指揮者は戦略・統括として振舞う。

## Plan Mode

Plan Mode で計画を作成する際、不明点や曖昧な要件があれば
AskUserQuestion ツールを積極的に使用してユーザーに確認すること。
推測で進めず、要件・制約・優先度を明確にしてから計画を立てること。

### ブレスト・フェーズ（Plan Mode 自動適用）

Plan Mode では、実装計画に入る前に以下のブレスト・フェーズを踏むこと:

1. **目的・制約の確認**: いきなり「どう作るか」に入らず、まず「なぜ作るか」「成功基準は何か」を確認する
2. **アプローチ比較**: 2-3の実現方法を洗い出し、トレードオフを比較してから1つに絞る
3. **段階的な質問**: 質問は1問ずつ行い、一度に複数の質問を投げない。選択肢を提示できる場合は選択式にする
4. **YAGNI の徹底**: 設計段階で不要な機能・拡張ポイントを削ぎ落とす

スキップ条件:
- バグ修正など設計余地がない単純作業
- ユーザーが「ブレスト不要」「計画だけ」と明示した場合
